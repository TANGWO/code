package com.cl.main;

import java.util.Scanner;

public class AIMain {
	
	
	@SuppressWarnings("resource")
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
		
		String str;
		
		while(true) {
			str=scanner.next();
			str=str.replace(Target: "Âð", repl "");
			str=str.replace(Target: "Âð", newChar);
			str=str.replace(Target: "Âð", newChar);
			str=str.replace(Target: "Âð", newChar);
					
			
			
		}
		
	}

}
